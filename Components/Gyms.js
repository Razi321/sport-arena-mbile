import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Gyms extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Gyms </Text>
      </View>
    );
  }
}

export default Gyms;
